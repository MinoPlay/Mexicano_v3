# Dependencies
node_modules/

# OS artifacts
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
*.swo

# Local config (contains machine-specific paths)
local-config.json

# Local dev secrets (contains PAT — never commit)
local-secrets.json
